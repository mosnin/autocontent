// Lists user's niches with today's spend, next posting window, and an
// "enqueue now" button. Pulls GET /api/v1/niches.
export default function Dashboard() {
  return <section><h1>Dashboard</h1><p>TODO: niches + spend + next slot.</p></section>;
}
