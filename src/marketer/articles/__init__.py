"""Written-content / SEO pipeline.

The second half of marketer.sh: alongside the video pipeline, agents can
produce publication-ready, SEO-optimized articles — SERP research,
structured outline, section-parallel writing, QA scoring with one
corrective rewrite, metadata + JSON-LD schema, and an optional hero
image. Every LLM and image call is metered through the same
SpendContext/caps/ledger as the video pipeline.
"""
