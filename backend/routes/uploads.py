"""Media uploads into the library (Team Storyboard-Uploads).

Registered in main.py; endpoints are implemented by the owning team.
"""

from __future__ import annotations

from fastapi import APIRouter

router = APIRouter()
