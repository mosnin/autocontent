"""Google Search Console — OAuth connect, rankings, queries, content gaps (Team GSC).

Registered in main.py; endpoints are implemented by the owning team.
"""

from __future__ import annotations

from fastapi import APIRouter

router = APIRouter()
