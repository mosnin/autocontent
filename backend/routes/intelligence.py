"""Content intelligence — clusters, corpus audit, cannibalization (Team Content-Intel).

Registered in main.py; endpoints are implemented by the owning team.
"""

from __future__ import annotations

from fastapi import APIRouter

router = APIRouter()
